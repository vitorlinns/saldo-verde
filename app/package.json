{
  "name": "saldo-verde-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --workspace app/frontend run dev",
    "build": "npm --workspace app/frontend run build && npm --workspace app/backend run build",
    "start": "npm --workspace app/frontend run start",
    "lint": "npm --workspace app/frontend run lint"
  }
}
